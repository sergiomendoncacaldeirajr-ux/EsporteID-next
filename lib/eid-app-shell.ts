/** Request header set by `lib/supabase/middleware.ts` (chamado a partir de `proxy.ts`) — lido em `app/layout.tsx`. */
export const EID_HIDE_APP_SHELL_HEADER = "x-eid-hide-app-shell";
/** Header explícito para liberar topbar mínima APENAS no onboarding. */
export const EID_SHOW_ONBOARDING_CHROME_HEADER = "x-eid-show-onboarding-chrome";
